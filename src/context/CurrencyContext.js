import { createContext } from "react";

export const CurrencyContext = createContext();             /* Creates a context object. This context object 
                                                               will help us to manage the global data store. */